{
  "name": "SampleApp",
  "version": "1.0.0",
  "description": "Simple Node Application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
